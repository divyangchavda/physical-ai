[pytest]
# Only collect our own tests. Without this, pytest crawls the vendored
# GroundingDINO/ clone and dies during collection on its demo scripts.
testpaths = tests
norecursedirs = GroundingDINO data runs weights output_* .git __pycache__ tools/scratch
