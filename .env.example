# Copy to .env and fill in. NEVER commit .env — it is gitignored.
#
# On Kaggle you do not need this file: add the key under
#   Add-ons -> Secrets -> GEMINI_API_KEY
# and the notebook writes .env for you.

GEMINI_API_KEY=your_key_here
